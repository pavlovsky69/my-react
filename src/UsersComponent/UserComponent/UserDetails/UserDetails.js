import React from 'react';
import App from "../../../App";

const UserDetails = (props) => {
const {users}
    {
        users.map((user, id))=>{

    }
    }
    return (
        <>
            <div>
            </div>

        </>);
};

export default UserDetails;